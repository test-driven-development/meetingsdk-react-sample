{
  "name": "meetingsdk-sample-react",
  "version": "2.9.7",
  "author": "Zoom Video Communications, Inc.",
  "contributors": [
    {
      "name": "Tommy Gaessler"
    }
  ],
  "homepage": "",
  "private": true,
  "dependencies": {
    "@testing-library/jest-dom": "^5.11.4",
    "@testing-library/react": "^11.1.0",
    "@testing-library/user-event": "^12.1.10",
    "@zoomus/websdk": "^2.9.7",
    "react": "^17.0.1",
    "react-dev-utils": "^11.0.4",
    "react-dom": "^17.0.1",
    "react-scripts": "4.0.3",
    "web-vitals": "^0.2.4"
  },
  "scripts": {
    "start": "react-scripts start",
    "build": "react-scripts build",
    "test": "react-scripts test",
    "eject": "react-scripts eject",
    "build:dev": "yarn --frozen-lockfile && yarn validate",
    "lint": "eslint --ignore-path .gitignore --ext .js,.jsx,.ts,.tsx src",
    "format": "prettier --ignore-path .gitignore --write src public",
    "test:ci": "react-scripts test --watchAll=false",
    "build:ci": "yarn build --frozen-lockfile --production",
    "tdd": "react-scripts test",
    "validate": "yarn lint && yarn format && yarn build"
  },
  "eslintConfig": {
    "extends": [
      "react-app",
      "react-app/jest",
      "prettier"
    ]
  },
  "browserslist": {
    "production": [
      ">0.2%",
      "not dead",
      "not op_mini all"
    ],
    "development": [
      "last 1 chrome version",
      "last 1 firefox version",
      "last 1 safari version"
    ]
  },
  "volta": {
    "node": "16.19.0"
  },
  "devDependencies": {
    "eslint-config-prettier": "^8.6.0",
    "prettier": "^2.8.3"
  }
}
